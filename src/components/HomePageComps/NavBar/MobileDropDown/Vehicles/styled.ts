import styled from "styled-components";

const AvContainer = styled.section`
  position: absolute;
  transform: translateX(-100%);
  transition: transform 0.2s;
  top: 0;
  left: 0;
  right: 0;
  z-index: 300;
`;

export { AvContainer };
