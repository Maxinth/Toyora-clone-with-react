import { Container, InnerContainer } from "./styled";
import Logo from "./Logo";

const MainNav = () => {
  return (
    <Container>
      <InnerContainer>
        <Logo />
      </InnerContainer>
    </Container>
  );
};

export default MainNav;
